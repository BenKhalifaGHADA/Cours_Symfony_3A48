<html>
<head>
    <body>
    <table>
        <tr>
            <td>ID</td>
            <td>Username</td>
            <td>Email</td>
            <td>Actions</td>
        </tr>
        {% for author in Authors %}
        <tr>
            <td>{{ author.id }}</td>
            <td>{{ author.username }}</td>
            <td>{{ author.email }}</td>
            <td><a href="{{ path('updateAuthor',{'id':author.id})}}">update</a>
                 <a href="{{ path('deleteAuthor',{'id':author.id}) }}">Delete</a>
                <a href="{{ path('showA',{'id':author.id}) }}">show</a>
            </td>
        </tr>
        {% endfor %}
    </table>
</body>
</head>
</html>